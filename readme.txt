Packages required

Pip install selenium 
Pip install csv
Pip install pytresseract
Pip install PIL
Others :time,re,sys,datetime


Initially the driver starts the start() method start method calls login method enter details on the home page and get the screen shot of the captcha image. 
Ocr outputs the text format if the format is according to regex(in program) then the next page is loaded.

chromedriver version 80
python version 3.6
